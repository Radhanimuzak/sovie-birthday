const finalMessageScene=document.getElementById('final-message-scene'),viviAlbum=document.getElementById('vivi-album'),finalLightbox=document.getElementById('final-photo-lightbox'),finalLightboxImage=document.getElementById('final-lightbox-image'),finalLightboxClose=document.getElementById('final-lightbox-close'),viviPhotos=Array.from({length:32},(_,i)=>`vivi ${i+1}.jpeg`),photoPath=f=>`assets/images/${f}`;let finalSceneObserver;
function photoButton(f){return `<button class="wall-photo" type="button" data-photo="${f}" aria-label="Open photo"><img src="${photoPath(f)}" alt="Vivi" loading="lazy"></button>`}function closeFinalLightbox(){finalLightbox.classList.remove('is-open');finalLightbox.setAttribute('aria-hidden','true');document.body.classList.remove('final-lightbox-open')}function openFinalLightbox(f){finalLightboxImage.src=photoPath(f);finalLightbox.classList.add('is-open');finalLightbox.setAttribute('aria-hidden','false');document.body.classList.add('final-lightbox-open');finalLightboxClose.focus()}
function initializeFinalMessageScene(){if(!viviAlbum||viviAlbum.children.length)return;let a=viviPhotos.slice(0,16),b=viviPhotos.slice(16);viviAlbum.innerHTML=`<div class="memory-wall-row wall-left"><div class="memory-wall-track">${a.map(photoButton).join('')}${a.map(photoButton).join('')}</div></div><div class="memory-wall-row wall-right"><div class="memory-wall-track">${b.map(photoButton).join('')}${b.map(photoButton).join('')}</div></div>`;viviAlbum.querySelectorAll('.wall-photo').forEach(x=>x.onclick=()=>openFinalLightbox(x.dataset.photo));finalSceneObserver=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){e.target.classList.add('is-visible');finalSceneObserver.unobserve(e.target)}}),{threshold:.12});let t=document.querySelector('#final-message-scene .album-to-letter');if(t)finalSceneObserver.observe(t)}
finalLightboxClose.onclick=closeFinalLightbox;finalLightbox.onclick=e=>{if(e.target===finalLightbox)closeFinalLightbox()};document.addEventListener('keydown',e=>{if(e.key==='Escape')closeFinalLightbox()});if(finalMessageScene.classList.contains('active'))initializeFinalMessageScene();
